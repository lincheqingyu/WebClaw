# Claude 上下文压缩参考项目探索

更新日期：2026-04-08

## 1. 探索目标

这份文档只做一件事：

> 记录 Claude Code 反编译源码中，与上下文压缩和上下文工程最相关的参考点。

主参考路径：

- `/Users/hqy/Documents/zxh/github/Kuberwastaken-src`

## 2. 必读文件

### 主入口与上下文组织

- `main.tsx`
- `QueryEngine.ts`
- `history.ts`

### prompt 与 section 组织

- `constants/prompts.ts`
- `constants/systemPromptSections.ts`
- `utils/systemPrompt.ts`
- `utils/queryContext.ts`

### 记忆与 consolidation

- `services/autoDream/consolidationPrompt.ts`
- `memdir/memoryTypes.ts`

## 3. 重点观察点

后续探索时，优先回答这 6 个问题：

1. Claude Code 的上下文构建总入口在哪里
2. 哪些区块被视为静态前缀
3. 哪些区块被视为动态 section
4. compact / pruning / summary 是如何进入上下文的
5. side question / fallback / retry 是否共用同一套组装顺序
6. 哪些设计适合 Lecquy 复刻，哪些不适合

## 4. 建议的探索输出

每次新开线程做这条路线的探索时，优先输出下面三样东西：

- 上下文拼接流程图
- 对标差异表
- 可执行的下一步改动清单

## 5. 当前已知的借鉴点

- system prompt 不是单字符串，而是分块组织
- 静态块和动态块存在显式边界
- 上下文工程强调组装顺序一致性
- compact 的价值不只是压缩 token，而是维持共享前缀稳定

## 6. 当前已知的不宜照搬点

- 巨型单体入口结构
- 文件系统中心的长期记忆底座
- 依赖 Anthropic 专有服务端行为的部分
